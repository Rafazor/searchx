export interface ISuggestion {
    id: string;
    title: string;
    isHistory?: boolean;
}

export interface ISearchElement {
    id: string;
    title: string;
    description: string;
    url: string;
}
